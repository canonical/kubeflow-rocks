# Source https://github.com/kubeflow/notebooks/blob/notebooks-v1/components/tensorboard-controller/Dockerfile
name: tensorboard-controller
summary: An image for tensorboard controller
description: |
  This image is used as part of the Charmed Kubeflow product. The controller needs 
  for providing the measurements and visualizations needed during the machine learning workflow.
version: 1.11.0
license: Apache-2.0
base: ubuntu@24.04
services:
  tensorboard-controller:
    override: replace
    summary: "tensorboard-controller service"
    startup: enabled
    command: "/manager"
platforms:
  amd64:
run-user: _daemon_

parts:
  security-team-requirement:
    plugin: nil
    override-build: |
      mkdir -p ${CRAFT_PART_INSTALL}/usr/share/rocks
      (echo "# os-release" && cat /etc/os-release && echo "# dpkg-query" && \
       dpkg-query -f '${db:Status-Abbrev},${binary:Package},${Version},${source:Package},${Source:Version}\n' -W) > \
       ${CRAFT_PART_INSTALL}/usr/share/rocks/dpkg.query

  tensorboard-controller:
    plugin: go
    source: https://github.com/canonical/kubeflow-notebooks
    source-depth: 1
    source-type: git
    source-branch: notebooks-v1
    source-subdir: components/tensorboard-controller
    build-snaps:
      - go/1.24/stable
    build-packages:
      - apt
      - bash
    build-environment:
      - CGO_ENABLED: 0
      - GOOS: linux
    stage-packages:
      - bash
    organize:
      bin/tensorboard-controller: "/manager"
