integrations:
  - consumer-repository: https://github.com/canonical/kubeflow-dashboard-operator.git

    replace-image:
      - file: metadata.yaml
        path: resources.oci-image.upstream-source

    service-spec:
      - file: src/service-config.yaml
        command:
          path: command
          value: "npm start"
